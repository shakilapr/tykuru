// Placeholder so the Rust test gate (`cargo test`) has something to run in the
// baseline stage. Real integration tests driving the Typst sidecar are added
// in later stages (see work-plan.md Stage 3+).
#[test]
fn baseline_backend_compiles_and_runs() {
    assert!(true);
}
