// Event-name constants emitted by the backend and listened to on the frontend.
// These are wired up in later stages; declared now so all names live in one place.

export const SESSION_OPENED = "session-opened";
export const SESSION_CLOSED = "session-closed";
export const PREVIEW_UPDATED = "preview-updated";
export const COMPILE_STATE_CHANGED = "compile-state-changed";
